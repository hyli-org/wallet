id = "wallet"
log_format = "full"

data_directory = "data"
da_read_from = "127.0.0.1:4141"

rest_server_port = 4000
rest_server_max_body_size = 10_485_760 # 10 MB
node_url = "http://localhost:4321"

wallet_state_path = "wallet_state.json"
wallet_buffer_blocks = 0
wallet_max_txs_per_proof = 100
wallet_tx_working_window_size = 500
smt_buffer_blocks = 0
smt_max_txs_per_proof = 30
smt_tx_working_window_size = 150

# Invites
db_url = "postgres://postgres:postgres@localhost:5432/postgres"

[websocket]
port = 8081
ws_path = "/ws"
health_path = "/ws_health"
peer_check_interval.secs = 0
peer_check_interval.nanos = 100_000_000
